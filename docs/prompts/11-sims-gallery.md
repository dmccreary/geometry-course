# MicroSims Gallery

```linenums="0"
Please create a new Python program that generates a gallery page
for a website.  I have a set of directory at the following relative path:

ls ../../docs/sims/*

Each directory has a name, which we will call DIRNAME.
For each directory do the following:

Create a 